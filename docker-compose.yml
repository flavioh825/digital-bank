version: '3.8'

services:
  db:
    image: postgres:15
    container_name: bank_db
    environment:
      POSTGRES_DB: bank_db
      POSTGRES_USER: bank_db_user
      POSTGRES_PASSWORD: password@123
    ports:
      - "5432:5432"